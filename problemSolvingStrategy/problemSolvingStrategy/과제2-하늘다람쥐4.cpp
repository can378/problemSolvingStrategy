#include<iostream>
#include<vector>
using namespace std;

#define SIZE 200000


int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	//input===============
	long n;
	cin >> n;


	vector<long long> x(SIZE);
	vector<long long> y(SIZE);
	



	for (int i = 0; i < n; i++)
	{
		
		for (int j = 0; j < n; j++)
		{
			if (y[i] < y[j]) {}

		}
	}

}